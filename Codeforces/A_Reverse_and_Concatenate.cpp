#include <bits/stdc++.h>
using namespace std;
int main()
{
ios_base::sync_with_stdio(false);
cin.tie(NULL);
typedef long long ll;

ll t;
cin>>t;
while(t--)
{
  ll n,k;
  cin>>n>>k;
  string s;
  cin>>s;
  string r=s;
  reverse(s.begin(),s.end());
  ll ans=0;
  while(k--)
  {
    string r=s;
    reverse(s.begin(),s.end());
    
  }
}

return 0;
}